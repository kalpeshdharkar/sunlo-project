# Step 1: Remove already tracked large/unwanted files
git rm -r --cached node_modules
git rm -r --cached build
git rm --cached firebase_bin

# Step 2: Add .gitignore changes
git add .gitignore

# Step 3: Commit changes
git commit -m "Ignore node_modules, build, logs, firebase_bin, and cache files"

# Step 4: Push to GitHub
git push -u origin main
